const express = require('express');
const router = express.Router();

const sample1 = require();
const sample2 = require();
const error = require('./error');


router.get('',);

router.post('',);




router.use(error.client);
router.use(error.server);

module.exports = router;
